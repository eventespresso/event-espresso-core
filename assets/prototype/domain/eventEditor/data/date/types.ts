export enum DatesSortedBy {
	chronologically = 'chronologically',
	'by-name' = 'by-name',
	'by-id' = 'by-id',
	'by-order' = 'by-order',
}

export enum DisplayDates {
	start = 'start',
	end = 'end',
	both = 'both',
}

export enum ShowDates {
	all = 'all',
	'active-upcoming' = 'active-upcoming',
	'active-only' = 'active-only',
	'upcoming-only' = 'upcoming-only',
	'next-active-upcoming-only' = 'next-active-upcoming-only',
	'sold-out-only' = 'sold-out-only',
	'above-90-capacity' = 'above-90-capacity',
	'above-75-capacity' = 'above-75-capacity',
	'above-50-capacity' = 'above-50-capacity',
	'below-50-capacity' = 'below-50-capacity',
	'recently-expired-only' = 'recently-expired-only',
	'expired-only' = 'expired-only',
	'trashed-only' = 'trashed-only',
}
