/**
 * Event Espresso WP GraphQL namespace
 */
export const GQL_NAMESPACE = 'Espresso';

/**
 * Namespace with lcfirst()
 */
export const GQL_NAMESPACE_LCF = GQL_NAMESPACE.charAt(0).toLowerCase() + GQL_NAMESPACE.substr(1);
