/**
 * @function
 * @param {Object} eventDate    JSON object defining the Event Date
 */
export const trashEventDate = ( eventDate ) => {
	console.log( ' >>> CLICK <<< TRASH EVENT DATE ', eventDate );
};
