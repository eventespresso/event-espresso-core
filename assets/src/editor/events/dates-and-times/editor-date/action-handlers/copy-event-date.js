/**
 * @function
 * @param {Object} eventDate    JSON object defining the Event Date
 */
export const copyEventDate = ( eventDate ) => {
	console.log( ' >>> CLICK <<< COPY EVENT DATE ', eventDate );
};
