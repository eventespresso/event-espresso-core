/**
 * Internal imports
 */
import {
	SHOW_ARCHIVED_DATES,
	SHOW_EXPIRED_DATES,
	SHOW_ARCHIVED_TICKETS,
	SHOW_EXPIRED_TICKETS,
} from './constants';

export const showArchivedDatesAction = { type: SHOW_ARCHIVED_DATES };
export const showExpiredDatesAction = { type: SHOW_EXPIRED_DATES };
export const showArchivedTicketsAction = { type: SHOW_ARCHIVED_TICKETS };
export const showExpiredTicketsAction = { type: SHOW_EXPIRED_TICKETS };
