import data from './data';

/**
 * Provided via the data passed along by the server.
 * This data is any settings information for accounting js passed along from
 * the server.
 * @type {{}}
 */
export const { money = {} } = data;
